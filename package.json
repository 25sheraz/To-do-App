{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Minimal todo app — SHE-5",
  "scripts": {
    "test": "jest --testEnvironment jsdom"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
