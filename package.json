{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Minimal todo app — SHE-5",
  "scripts": {
    "test": "jest --testEnvironment jsdom",
    "lint": "eslint app.js mcp-server/**/*.js tests/**/*.js",
    "mcp": "node mcp-server/index.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
